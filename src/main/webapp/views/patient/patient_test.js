'use strict';

describe('lastMinuteDoctor.patient module', function() {

  beforeEach(module('lastMinuteDoctor.patient'));

  describe('patient controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var patientCtrl = $controller('patientCtrl');
      expect(patientCtrl).toBeDefined();
    }));

  });
});